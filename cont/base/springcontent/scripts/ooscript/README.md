# OOScript (Lua + OOP)

OOScript is a lightweight object-oriented layer built on Lua for ArcLight gameplay code.

Core modules:
- ooclass.lua: class creation and inheritance
- runtime.lua: plugin/script lifecycle registry
- example_player.lua: sample component-style gameplay script

This keeps Lua as the runtime language while enabling OOP structure.
