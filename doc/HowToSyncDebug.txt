How to use the sync debugger (for testers)

ArcLight Engine sync debugging:

1) Reproduce the desync by running a replay in debug mode
2) Enable sync logging in the engine config
3) Compare sync logs between clients to identify the divergence point
4) Use the built-in sync debugger to step through the simulation frame by frame

For detailed instructions, see the project documentation.
